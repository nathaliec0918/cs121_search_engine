# cs121_search_engine

1. run the program without system arguments
2. terminal will prompt asking whether the documents have been indexed or not already
3. if already indexed, provide the directory of the partial indexes and the number of documents in its corpus
   if not, provide the directory of the documents to index
4. then, proceed with search
